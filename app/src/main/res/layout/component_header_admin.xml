<?xml version="1.0" encoding="utf-8"?>
<!--
    Componente de cabecera común para las pantallas principales del rol Admin.
    Contiene: nombre de inmobiliaria + botón notificaciones + avatar.
    El título de página (Proyectos, Separaciones, etc.) se declara en cada
    layout anfitrión inmediatamente después del <include>, ya que varía por pantalla.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical">

    <TextView
        android:id="@+id/tvRealEstateName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="Inmobiliaria"
        android:textColor="@color/white"
        android:textSize="14sp" />

    <ImageButton
        android:id="@+id/btnNotifications"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginEnd="12dp"
        android:background="@android:color/transparent"
        android:contentDescription="Notificaciones"
        android:padding="8dp"
        android:src="@drawable/ic_notifications"
        android:tint="@color/white" />

    <TextView
        android:id="@+id/tvAvatar"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:gravity="center"
        android:background="@drawable/onboarding_skip_bg"
        android:backgroundTint="@color/brand_lime"
        android:text="JG"
        android:textColor="@color/white"
        android:textStyle="bold" />

</LinearLayout>
