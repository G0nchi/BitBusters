<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permisos -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BitBusters">
        <activity
            android:name=".activities.cliente.SearchActivity"
            android:exported="false" />
        <activity
            android:name=".activities.cliente.PaymentMethodActivity"
            android:exported="false" />
        <activity
            android:name=".activities.cliente.AgendaCitaActivity"
            android:exported="false" />
        <activity
            android:name=".activities.cliente.MisCitasActivity"
            android:exported="false" />
        <activity
            android:name=".activities.cliente.ReviewsActivity"
            android:exported="false" />
        <activity
            android:name=".activities.cliente.ViewOnMapActivity"
            android:exported="false" /> <!-- API Key de Google Maps — DEBE ir dentro de <application> -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="TU_API_KEY_AQUI" /> <!-- Pantalla de inicio -->
        <activity
            android:name=".activities.access.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Onboarding -->
        <activity
            android:name=".activities.access.OnboardingTourActivity"
            android:exported="false" /> <!-- Cliente -->
        <activity
            android:name=".activities.cliente.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".activities.cliente.ProjectDetailActivity"
            android:exported="false" />
        <activity
            android:name=".activities.cliente.AddCommentActivity"
            android:exported="false" />
        <activity
            android:name=".activities.cliente.NotificationsActivity"
            android:exported="false" />
        <activity
            android:name=".activities.cliente.NotificationsActivity"
            android:exported="false" />
        <activity
            android:name=".activities.cliente.MessagesActivity"
            android:exported="false" />
        <activity
            android:name=".activities.cliente.MessagesActivity"
            android:exported="false" />
        <activity
            android:name=".activities.cliente.ChatDetailActivity"
            android:exported="false" />
        <activity
            android:name=".activities.cliente.ChatDetailActivity"
            android:exported="false" />
    </application>

</manifest>